# Build system (Rust-based; see zensical.toml for configuration)
zensical

# Required: zensical hard-errors at build time if [project.plugins.mkdocstrings]
# is declared in zensical.toml and this package is missing.
mkdocstrings

# Declared as [project.plugins.git-revision-date] in zensical.toml. Not currently
# enforced by zensical and emits no output, but kept in sync with the config.
mkdocs-git-revision-date-plugin
